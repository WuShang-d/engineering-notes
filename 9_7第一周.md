# [Git](https://missing.csail.mit.edu/2026/version-control/)

版本控制工具，主要特点是能够快速查看修改历史，同时方便并行开发多个项目功能。

## Matadata

主要是 tree 和 blob，tree 可以包含 tree，因此使得文件夹中可以再有文件夹。

## 历史记录

o <- o <- o

# Vim

方便的编辑器工具，主要目的是为了摒弃掉效率较低的鼠标。

# Mini Swe Agent

## 找到程序入口

在根目录下 bash 输入 `mini` 能够直接启动该项目，因此寻找 `.venv/bin/mini` ，代码如下：

	import sys
	from minisweagent.run.mini import app
	if __name__ == "__main__":
    	if sys.argv[0].endswith("-script.pyw"):
        	sys.argv[0] = sys.argv[0][:-11]
    	elif sys.argv[0].endswith(".exe"):
        	sys.argv[0] = sys.argv[0][:-4]
    	sys.exit(app())

主要是 `import app`，然后解析了参数，最终构造了一个 `app()`。因此前往查找 `minisweagent/run/mini.py` 

如它正文第一行所示，这是一个默认的可执行入口，因此推断是代码入口：

	"""Run mini-SWE-agent in your local environment. This is the default executable `mini`."""

参看 `main()` 函数的输入参数，主要是：模型名、模型类别、Agent类别、任务等，因此判断这就是代码的入口。

## 找到 Agent.run()/主 Agent Loop

在 `minisweagent/run/mini.py` 中找到了 `agent.run(run_task)` ，也就是 `Agent.run()`，发现 `agent` 是从 `get_agent()` 返回的，因此直接查看对应函数。

	def get_agent(model: Model, env: Environment, config: dict, *, default_type: str = "") -> Agent:
    	config = copy.deepcopy(config)
    	agent_class = get_agent_class(config.pop("agent_class", default_type))
    	return agent_class(model, env, **config)

然后转到定义可以看到， `agents/default.py` 里面的 `class DefaultAgent` 下就有 `run()`，正是需要的函数。其中就有主 Loop。

    def run(self, task: str = "", **kwargs) -> dict:
        """Run step() until agent is finished. Returns dictionary with exit_status, submission keys."""
        self.extra_template_vars |= {"task": task, **kwargs}
        self.messages = []
        self.add_messages(
            self.model.format_message(role="system", content=self._render_template(self.config.system_template)),
            self.model.format_message(role="user", content=self._render_template(self.config.instance_template)),
        )
        while True:
            try:
                self.step()
                self.n_consecutive_format_errors = 0  # reset on any clean step
            except FormatError as e:
                # The call was billed before parsing failed, so query() never got to charge it.
                self.cost += e.messages[0].get("extra", {}).get("cost", 0.0)
                self.n_consecutive_format_errors += 1
                if 0 < self.config.max_consecutive_format_errors <= self.n_consecutive_format_errors:
                    self.add_messages(
                        *e.messages,
                        {
                            "role": "exit",
                            "content": "RepeatedFormatError",
                            "extra": {"exit_status": "RepeatedFormatError", "submission": ""},
                        },
                    )
                else:
                    self.add_messages(*e.messages)
            except InterruptAgentFlow as e:
                self.add_messages(*e.messages)
            except Exception as e:
                self.handle_uncaught_exception(e)
                raise
            finally:
                self.save(self.config.output_path)
            if self.messages[-1].get("role") == "exit":
                break
        return self.messages[-1].get("extra", {})


## 搞清用户输入如何进入第一轮 messages

核心在于 `minisweagent/run/mini.py` 中的如下代码：

    if (run_task := config.get("run", {}).get("task", UNSET)) is UNSET:
        console.print("[bold yellow]What do you want to do?")
        run_task = _multiline_prompt()
        console.print("[bold green]Got that, thanks![/bold green]")

首先提示用户需要干什么？然后用户输入的内容会被 `_multiline_prompt()` 方法一起整合返回 `run_task`，进入第一轮 messages

实测修改提问代码为 `What do you want to do with me?` 后启动项目，确实打印为修改后的内容，因此入口没错。

# 本周产出

## mini-swe-agent.md 第一版：系统解决什么问题、入口在哪里、核心对象有哪些

~~系统主要解决有关 github issues 的问题，然后主要是通过内置了一段 prompt。~~ 

mini-SWE-agent 更合适的理解是：一个极简的软件工程 Agent 框架，让 LLM 在一个执行环境里读取/修改代码、执行命令，并通过循环完成软件工程任务。GitHub issue / SWE-bench 只是它非常典型的任务来源，而不是它的适用范围。官方甚至直接用 agent.run("Write a sudoku game") 演示普通 coding task。

入口就在 `minisweagent/run/mini.py`

核心对象主要是 `run` 文件夹、`agent` 文件夹。

## 画出“用户输入 → Agent.run → 第一轮模型请求”的调用链

用户根据提示此输入 prompt。然后 prompt在 `minisweagent/run/mini.py` 中与 system prompt 组合为 messages，作为 `run_task` 输入到 `agent.run()` 中进入 agent loop。

## 本周补充理解

### mini-SWE-agent 的基本架构

mini-SWE-agent 并不只用于解决 GitHub Issues，更准确地说，它是一个精简的软件工程 Agent：模型根据当前 messages 进行推理，在 Environment 中执行操作，把执行结果重新加入上下文，并不断循环，直到任务完成。

目前可以将系统粗略拆成几个核心部分：

* **Run / CLI**：负责接收用户任务、加载配置以及组装组件。
* **Agent**：负责维护状态以及控制 Agent Loop。
* **Model**：负责真正向 LLM 发起请求。
* **Environment**：负责执行 Agent 产生的操作。
* **Config**：负责配置 Agent、Model、Environment 和运行方式。
* **messages**：Agent 运行期间最核心的状态之一，保存模型能够看到的上下文。

### 第一轮模型调用链

目前追踪得到的调用链为：

`用户输入`
→ `_multiline_prompt()`
→ `run_task`
→ `agent.run(run_task)`
→ 渲染 `system_template` 和 `instance_template`
→ 构造 `self.messages`
→ `step()`
→ `query()`
→ `model.query(self.messages)`
→ 第一轮模型请求

因此，`run_task` 本身只是用户输入的任务字符串；真正的 messages 是进入 `Agent.run()` 以后根据 task 和 prompt template 构造出来的。

### Git 数据模型

目前先将 Git 理解为四个核心概念：

`blob` 保存文件内容，`tree` 保存目录结构，`commit` 表示一次项目快照并记录父 commit，`ref` 则是指向 commit 的名字，例如 branch。

因此 commit 历史中的箭头表示当前 commit 指向它的 parent。

### Vim

Vim 的核心价值不只是避免使用鼠标，而是通过 modal editing 以及 operator + motion 的组合，让文本编辑操作变得可组合、可重复。
